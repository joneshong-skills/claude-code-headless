# Observations — claude-code-headless

## Pending

## Resolved

### 2026-02-11 — npm install deprecated, native installer recommended (applied in v0.1.0)
- **Resolution**: DeepWiki confirmed npm install deprecated since v2.1.15. Updated install instructions to `curl -fsSL https://claude.ai/install.sh | bash` and `brew install --cask claude-code`. npm still works but is no longer recommended.
